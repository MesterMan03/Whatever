{
  "name": "whatever-engine",
  "private": true,
  "workspaces": ["runtime"],
  "scripts": {
    "build:core": "cd mods/core && bun build src/index.ts --outfile scripts/index.js --minify --external=@whatever-engine/api",
    "build:runtime": "bun run --cwd runtime build:types"
  },
  "dependencies": {
    "@types/bun": "^1.3.14"
  }
}
