{
  "name": "whatever-engine",
  "private": true,
  "workspaces": ["runtime"],
  "scripts": {
    "build:core": "cd mods/core && bun build src/index.ts --outfile scripts/index.js --minify --external=@whatever/api",
    "build:runtime": "bun run --cwd runtime build:types"
  }
}
