{
  "name": "whatever-engine",
  "private": true,
  "workspaces": ["runtime"],
  "scripts": {
    "build:core": "cd mods/core && bun run build",
    "build:runtime": "bun run --cwd runtime build"
  },
  "dependencies": {
    "@types/bun": "^1.3.14"
  }
}
